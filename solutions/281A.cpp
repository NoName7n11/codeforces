// Word Capitalization

#include <bits/stdc++.h>
using namespace std;

int main(){
    string s;
    char u;
    cin >> s;

    char(toupper(s[0]));
    u = toupper(s[0]);
    cout << u << s.substr(1);
}
